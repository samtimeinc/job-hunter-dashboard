/** Small red "NEW" pill shown on rows the user hasn't acknowledged yet. */
export function NewBadge() {
  return (
    <span className="chip bg-new_badge-bg text-new_badge-text font-semibold uppercase tracking-wide">
      New
    </span>
  );
}
